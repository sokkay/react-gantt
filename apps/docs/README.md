# @sokkay/react-gantt docs

Vite React demo app for validating the local `@sokkay/react-gantt` package.

Run from the repository root:

```bash
pnpm dev
```

Build only this app:

```bash
pnpm --filter docs build
```
